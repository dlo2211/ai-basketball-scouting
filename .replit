language = "bash"
run = "bash -lc \".pythonlibs/bin/python -m streamlit run src/app.py \
  --server.port=$PORT \
  --server.address=0.0.0.0 \
  --server.headless=true \
  --server.enableCORS=false\""

# No need to manually map ports—Replit will forward $PORT → external 80 automatically
